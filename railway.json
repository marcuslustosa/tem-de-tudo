{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ALWAYS",
    "healthcheckPath": "/health",
    "preDeployCommand": "cd /var/www/html && php artisan migrate --force --no-interaction && php artisan app:ensure-demo-access --sync-passwords --no-interaction && php artisan app:verify-frontend-assets --fix --no-interaction"
  }
}
